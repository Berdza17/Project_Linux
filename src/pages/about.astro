---
/**
 * ABOUT PAGE
 * Company information and story.
 * 
 * Sections:
 * 1. Hero - Company name heading
 * 2. Company Story - Mission and background
 * 3. Why Choose Us - Key differentiators
 * 4. Contact CTA - Quick contact buttons
 */

import Layout from '../layouts/Layout.astro';
import { 
  siteConfig, 
  getWhatsAppLink, 
  getEmailLink,
  getYearsInBusiness 
} from '../config.js';

// Calculate years in business
const yearsInBusiness = getYearsInBusiness();
---

<Layout 
  title="O Nama" 
  description={`Saznajte više o ${siteConfig.companyName}. ${siteConfig.business.description}`}
>
  <!-- ============================================
       HERO SECTION
       ============================================ -->
  <section class="hero">
    <div class="hero-background"></div>
    <div class="hero-content">
      <div class="container">
        <h1 class="hero-title">{siteConfig.companyName}</h1>
        <p class="hero-tagline">Izrađujemo Kvalitetan Namještaj Od {siteConfig.business.establishedYear}</p>
      </div>
    </div>
  </section>

  <!-- ============================================
       COMPANY STORY SECTION
       ============================================ -->
  <section class="story-section">
    <div class="container">
      <div class="story-content">
        <header class="section-header">
          <h2 class="section-title">Naša Priča</h2>
          <div class="section-divider"></div>
        </header>

        <div class="story-text">
          <!-- 
            NAPOMENA ZA PRILAGOĐAVANJE:
            Uredite ovaj sadržaj da ispričate jedinstvenu priču vaše firme.
          -->
          <p class="lead-text">
            {siteConfig.business.description}
          </p>

          <p>
            Osnovana {siteConfig.business.establishedYear}. godine, {siteConfig.companyName} je započela sa jednostavnom 
            vizijom: kreirati lijep, funkcionalan namještaj koji kuće pretvara u domove. 
            Ono što je počelo kao mala radionica preraslo je u pouzdano ime u prilagođenom namještaju 
            širom Bosne i Hercegovine.
          </p>

          <p>
            Naše putovanje vodi strast prema obradi drveta i nepokolebljiva posvećenost 
            kvaliteti. Svaki komad koji kreiramo priča priču—vašu priču—izrađenu s preciznošću, 
            pažnjom i najboljim dostupnim materijalima.
          </p>

          <h3>Naša Misija</h3>
          <p>
            Vjerujemo da kvalitetan namještaj treba biti dostupan svima. Naša misija je 
            kombinirati tradicionalnu izradu sa modernim dizajnom, kreirajući komade koji nisu samo 
            lijepi već izgrađeni da traju generacijama. Radimo blisko sa svakim klijentom da 
            razumijemo njihovu viziju i oživimo je.
          </p>

          <h3>Zašto Radimo Ovo</h3>
          <p>
            Postoji nešto duboko zadovoljavajuće u transformaciji sirovog drveta u funkcionalno djelo 
            umjetnosti. Radimo ovo jer volimo vidjeti radost na licima naših klijenata kada vide 
            svoju prilagođenu kuhinju, trpezarijski sto iz snova, ili savršen ormar za njihovu spavaću sobu. 
            Svaki projekat je nova prilika da se stvori nešto značajno.
          </p>

          {yearsInBusiness > 0 && (
            <p class="highlight-text">
              Sa <strong>{yearsInBusiness} godina iskustva</strong>, završili smo stotine 
              projekata i izgradili trajne odnose sa klijentima koji nam se vraćaju iznova.
            </p>
          )}
        </div>
      </div>
    </div>
  </section>

  <!-- ============================================
       WHY CHOOSE US SECTION
       ============================================ -->
  <section class="why-section">
    <div class="container">
      <header class="section-header">
        <h2 class="section-title">Zašto Izabrati Nas</h2>
        <p class="section-subtitle">
          Šta nas razlikuje od konkurencije
        </p>
      </header>

      <div class="differentiators-grid">
        <!-- Differentiator 1 -->
        <article class="differentiator-card">
          <div class="differentiator-number">01</div>
          <h3 class="differentiator-title">Premium Kvalitetni Materijali</h3>
          <p class="differentiator-text">
            Nabavljamo samo najkvalitetnije drvo i materijale od pouzdanih dobavljača. 
            Svaki komad je pažljivo odabran za trajnost, ljepotu i održivost.
          </p>
          <ul class="differentiator-list">
            <li>Konstrukcija od masivnog drveta</li>
            <li>Premium okov i dodaci</li>
            <li>Ekološki prihvatljive završne obrade</li>
          </ul>
        </article>

        <!-- Differentiator 2 -->
        <article class="differentiator-card">
          <div class="differentiator-number">02</div>
          <h3 class="differentiator-title">100% Prilagođeni Dizajn</h3>
          <p class="differentiator-text">
            Bez šablona, bez kompromisa. Svaki komad je dizajniran posebno za 
            vaš prostor, vaše potrebe i vaše stilske preferencije.
          </p>
          <ul class="differentiator-list">
            <li>Besplatne dizajn konsultacije</li>
            <li>3D vizualizacija dostupna</li>
            <li>Neograničene revizije</li>
          </ul>
        </article>

        <!-- Differentiator 3 -->
        <article class="differentiator-card">
          <div class="differentiator-number">03</div>
          <h3 class="differentiator-title">Stručna Izrada</h3>
          <p class="differentiator-text">
            Naš tim vještih zanatlija donosi decenije kombinovanog iskustva 
            svakom projektu. Pažnja na detalje je naš potpis.
          </p>
          <ul class="differentiator-list">
            <li>Ručno izrađena preciznost</li>
            <li>Tradicionalne tehnike spajanja</li>
            <li>Kontrola kvaliteta na svakom koraku</li>
          </ul>
        </article>

        <!-- Differentiator 4 -->
        <article class="differentiator-card">
          <div class="differentiator-number">04</div>
          <h3 class="differentiator-title">Kompletna Usluga</h3>
          <p class="differentiator-text">
            Od početnog koncepta do finalne instalacije, mi brinemo o svemu. 
            Opustite se i gledajte kako vaša vizija postaje stvarnost.
          </p>
          <ul class="differentiator-list">
            <li>Profesionalna instalacija</li>
            <li>Garancija uključena</li>
            <li>Postprodajna podrška</li>
          </ul>
        </article>
      </div>
    </div>
  </section>

  <!-- ============================================
       LOCATION SECTION
       ============================================ -->
  <section class="location-section">
    <div class="container">
      <div class="location-content">
        <div class="location-info">
          <h2 class="section-title">Posjetite Našu Radionicu</h2>
          <p class="location-text">
            Radujemo se posjetiteljima u našoj radionici uz prethodni dogovor. Dođite vidjeti naše zanatlije na djelu 
            i razgovarajte o vašem projektu uživo.
          </p>
          <address class="location-address">
            <span class="location-icon" aria-hidden="true">📍</span>
            {siteConfig.address}
          </address>
          <p class="location-hours">
            <span class="location-icon" aria-hidden="true">🕒</span>
            {siteConfig.business.workingHours}
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- ============================================
       CTA SECTION
       ============================================ -->
  <section class="cta-section">
    <div class="container">
      <div class="cta-content">
        <h2 class="cta-title">Spremni da Započnete Vaš Projekat?</h2>
        <p class="cta-description">
          Kreirajmo nešto lijepo zajedno. Kontaktirajte nas danas za besplatne 
          konsultacije i ponudu—bez obaveza, samo sjajne ideje.
        </p>

        <div class="cta-buttons">
          <a 
            href={getWhatsAppLink('Zdravo! Posjetio sam vašu stranicu O Nama i želim razgovarati o projektu.')}
            target="_blank"
            rel="noopener noreferrer"
            class="btn btn-whatsapp"
          >
            <span class="btn-icon" aria-hidden="true">💬</span>
            WhatsApp
          </a>

          <a 
            href={getEmailLink('Upit za Projekat')}
            class="btn btn-email"
          >
            <span class="btn-icon" aria-hidden="true">✉️</span>
            Email
          </a>

          <a href="/contact" class="btn btn-outline">
            Kontakt Stranica
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* ============================================
     HERO SECTION
     ============================================ */
  .hero {
    position: relative;
    height: 100vh;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      135deg,
      var(--color-bg-tertiary) 0%,
      var(--color-bg-secondary) 50%,
      var(--color-bg) 100%
    );
    z-index: 0;
  }

  /* Optional: Add background image 
  .hero-background {
    background-image: url('/images/about-hero.jpg');
    background-size: cover;
    background-position: center;
  }
  .hero-background::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
  }
  */

  .hero-content {
    position: relative;
    z-index: 1;
    text-align: center;
    color: var(--color-text);
    padding: var(--space-48) 0;
  }

  .hero-title {
    font-size: var(--font-size-4xl);
    font-weight: var(--font-weight-bold);
    margin-bottom: var(--space-16);
    color: var(--color-text);
  }

  .hero-tagline {
    font-size: var(--font-size-xl);
    color: var(--color-text-secondary);
    margin: 0;
  }

  @media (min-width: 768px) {
    .hero-title {
      font-size: var(--font-size-6xl);
    }
    .hero-tagline {
      font-size: var(--font-size-2xl);
    }
  }

  /* ============================================
     SECTION COMMON STYLES
     ============================================ */
  .story-section,
  .why-section,
  .location-section {
    padding: var(--space-64) 0;
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--space-48);
  }

  .section-title {
    font-size: var(--font-size-3xl);
    margin-bottom: var(--space-16);
    color: var(--color-text);
  }

  .section-subtitle {
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    max-width: 600px;
    margin: 0 auto;
  }

  .section-divider {
    width: 80px;
    height: 4px;
    background-color: var(--color-primary);
    margin: var(--space-24) auto 0;
    border-radius: var(--radius-full);
  }

  /* ============================================
     STORY SECTION
     ============================================ */
  .story-section {
    background-color: var(--color-bg);
  }

  .story-content {
    max-width: 800px;
    margin: 0 auto;
  }

  .story-text p {
    font-size: var(--font-size-lg);
    line-height: var(--line-height-relaxed);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-24);
  }

  .story-text h3 {
    font-size: var(--font-size-xl);
    color: var(--color-text);
    margin-top: var(--space-40);
    margin-bottom: var(--space-16);
  }

  .lead-text {
    font-size: var(--font-size-xl) !important;
    color: var(--color-text) !important;
    font-weight: var(--font-weight-medium);
  }

  .highlight-text {
    background-color: var(--color-bg-secondary);
    padding: var(--space-24);
    border-left: 4px solid var(--color-primary);
    border-radius: 0 var(--radius-md) var(--radius-md) 0;
    font-style: italic;
  }

  /* ============================================
     WHY CHOOSE US SECTION
     ============================================ */
  .why-section {
    background-color: var(--color-bg-secondary);
  }

  .differentiators-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-32);
    align-items: stretch;
  }

  @media (min-width: 768px) {
    .differentiators-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .differentiator-card {
    background-color: var(--color-bg);
    padding: var(--space-32);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
    transition: all var(--transition-base);
    /* Ensure all cards have same width and stretch to fill grid cell */
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
  }

  .differentiator-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
  }

  .differentiator-number {
    font-size: var(--font-size-4xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-primary);
    opacity: 0.3;
    margin-bottom: var(--space-8);
    line-height: 1;
  }

  .differentiator-title {
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-text);
    margin-bottom: var(--space-12);
  }

  .differentiator-text {
    font-size: var(--font-size-base);
    color: var(--color-text-secondary);
    line-height: var(--line-height-relaxed);
    margin-bottom: var(--space-16);
    /* Allow text to grow and push list to bottom */
    flex-grow: 1;
  }

  .differentiator-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .differentiator-list li {
    position: relative;
    padding-left: var(--space-24);
    margin-bottom: var(--space-8);
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
  }

  .differentiator-list li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--color-primary);
    font-weight: var(--font-weight-bold);
  }

  /* ============================================
     LOCATION SECTION
     ============================================ */
  .location-section {
    background-color: var(--color-bg);
  }

  .location-content {
    max-width: 600px;
    margin: 0 auto;
    text-align: center;
  }

  .location-text {
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-32);
  }

  .location-address,
  .location-hours {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-12);
    font-size: var(--font-size-lg);
    color: var(--color-text);
    font-style: normal;
    margin-bottom: var(--space-12);
  }

  .location-icon {
    font-size: var(--font-size-2xl);
  }

  /* ============================================
     CTA SECTION
     ============================================ */
  .cta-section {
    background: linear-gradient(
      135deg,
      var(--color-bg-tertiary) 0%,
      var(--color-bg-secondary) 100%
    );
    padding: var(--space-64) 0;
  }

  .cta-content {
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
  }

  .cta-title {
    font-size: var(--font-size-3xl);
    margin-bottom: var(--space-16);
    color: var(--color-text);
  }

  .cta-description {
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-32);
  }

  .cta-buttons {
    display: flex;
    flex-direction: column;
    gap: var(--space-16);
    align-items: center;
  }

  @media (min-width: 640px) {
    .cta-buttons {
      flex-direction: row;
      justify-content: center;
      flex-wrap: wrap;
    }
  }

  /* ============================================
     BUTTONS
     ============================================ */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-8);
    padding: var(--space-16) var(--space-32);
    font-size: var(--font-size-base);
    font-weight: var(--font-weight-semibold);
    text-decoration: none;
    border-radius: var(--radius-md);
    transition: all var(--transition-base);
    cursor: pointer;
    border: 2px solid transparent;
    min-height: 48px;
    min-width: 180px;
  }

  .btn-whatsapp {
    background-color: #25D366;
    color: var(--color-text-light);
  }

  .btn-whatsapp:hover {
    background-color: #20BA5A;
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
    text-decoration: none;
  }

  .btn-email {
    background-color: var(--color-primary);
    color: var(--color-text-light);
  }

  .btn-email:hover {
    background-color: var(--color-primary-dark);
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
    text-decoration: none;
  }

  .btn-outline {
    background-color: transparent;
    color: var(--color-primary);
    border-color: var(--color-primary);
  }

  .btn-outline:hover {
    background-color: var(--color-primary);
    color: var(--color-text-light);
    text-decoration: none;
  }

  .btn-icon {
    font-size: var(--font-size-xl);
  }

  .btn:focus {
    outline: 3px solid white;
    outline-offset: 2px;
  }

  /* ============================================
     ACCESSIBILITY & REDUCED MOTION
     ============================================ */
  @media (prefers-reduced-motion: reduce) {
    .differentiator-card,
    .btn {
      transition: none;
    }
  }

  /* ============================================
     DARK MODE
     ============================================ */
  @media (prefers-color-scheme: dark) {
    .differentiator-card {
      background-color: var(--color-bg-secondary);
    }
    
    .highlight-text {
      background-color: var(--color-bg-tertiary);
    }
  }
</style>

